# Copy this file to .env and fill in your API keys
# Get Anthropic API key: https://console.anthropic.com
# Get Tavily API key: https://app.tavily.com (free tier: 1000 req/month)

ANTHROPIC_API_KEY=your-anthropic-api-key-here
TAVILY_API_KEY=your-tavily-api-key-here

# App login: per-user accounts, no env var needed. On first run (empty users
# table), open the app and it walks you through creating the first admin
# account (POST /api/auth/bootstrap).
SESSION_TTL_HOURS=12

# Optional: Fernet key for encrypting stored secrets at rest. Auto-generated
# next to the DB if unset. Generate:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# NEVER just swap this value once secrets exist — every already-encrypted
# secret becomes permanently undecryptable. To rotate safely (e.g. an
# ownership transfer), use scripts/rotate_secret_key.py — see README.md's
# "Secret Key Rotation" section.
SECRET_ENCRYPTION_KEY=

# Optional overrides (defaults shown)
DATABASE_URL=sqlite:///./data/research.db
CHROMA_PERSIST_DIR=./data/chroma
UPLOADS_DIR=./data/uploads
CLAUDE_MODEL=claude-opus-4-6
CLAUDE_FAST_MODEL=claude-haiku-4-5-20251001
MAX_SEARCH_RESULTS=10
CORS_ORIGINS=http://localhost:3000

# Daily Digest Email (optional)
# Gmail app password: Google Account → Security → 2-Step Verification → App passwords
DIGEST_EMAIL_TO=
DIGEST_EMAIL_FROM=
DIGEST_SMTP_PASSWORD=
DIGEST_TOPICS=AI policy,AI regulation,AI governance,AI safety
